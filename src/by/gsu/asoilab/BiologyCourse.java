package by.gsu.asoilab;

public class BiologyCourse extends Course {
    public BiologyCourse(int id, String name, int maxPoints, int threshold, int pointsToHonors) {
        super(id, name, maxPoints, threshold, pointsToHonors);
    }
}
